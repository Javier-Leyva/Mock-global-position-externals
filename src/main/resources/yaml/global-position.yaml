---
swagger: "2.0"
info:
  version: 1.0.3
  title: Global Position
  description: >-
    [HTML Docs](/specs/global-position/index.html)

    Provides information about contracts and balances of contracts of a
    customer. It also includes endpoints to allow customizing this list assigning
    an alias to each contract, or sorting the sequence of each product family in the
    list.
  x-ibm-name: global-position
basePath: /global_position
schemes:
- https
consumes:
- application/json
produces:
- application/json
paths:
  /:
    get:
      description: This operation allows us to obtain total balances by product families
        and contract balances. You can group balances by product family. It allows
        filters by family of products, by contractual situation (active, canceled).
        - if familyProduct is informed, the return will be only for this family -
        If active_only is true only active contracts take it, with the false return
        there will be active and canceled contracts.
      summary: Provides Santander information about contracts and balances of contracts of a customer.
      tags:
      - global-position
      operationId: MeGlobalPositionGetV3
      deprecated: false
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: familyProduct
        in: query
        required: false
        type: string
        description: Available values (accounts, cards, loans, deposits and securities)
        x-example: cards
      - name: activeOnly
        in: query
        required: true
        type: boolean
        description: Available values (true or false)
        x-example: true
      - name: customerId
        type: string
        required: false
        in: query
        description: customer_id
      - name: operation
        type: string
        in: query
        description: Operation value that need to be considered for filter
        required: false
        enum :
        - contracts
        - transferFrom
        - transferTo
        - makePayment
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/_GetGlobalPositionResponse'
        204:
          description: No Content
        400:
          description: Bad Request
          schema:
            $ref: '#/definitions/Errors'
        401:
          description: Unauthorized
          schema:
            $ref: '#/definitions/Errors'
        403:
          description: Forbidden
          schema:
            $ref: '#/definitions/Errors'
        404:
          description: Not Found
          schema:
            $ref: '#/definitions/Errors'
        500:
          description: Internal Server Error
          schema:
            $ref: '#/definitions/Errors'
        503:
          description: Service Unavailable
          schema:
            $ref: '#/definitions/Errors'
        504:
          description: Gateway Service Timeout
          schema:
            $ref: '#/definitions/Errors'
      security:
      - JWT Profile:
        - gblpos.read
        x-IBM-Client-Id: []
  /external:
    get:
      description: This operation allows us to obtain total balances by product families
        and contract balances. You can group balances by product family. It allows
        filters by family of products, by contractual situation (active, canceled).
        - if familyProduct is informed, the return will be only for this family -
        If active_only is true only active contracts take it, with the false return
        there will be active and canceled contracts.
      summary: Provides other banks information about contracts and balances of contracts of a customer.
      tags:
      - global-position
      operationId: ExternalGlobalPositionGet
      deprecated: false
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: bank_id
        type: string
        required: true
        in: query
        description : "Unique bank ID"
        x-example : "AAOZ"
      - name: familyProduct
        in: query
        required: false
        type: string
        description: Available values (accounts, cards, loans, deposits and securities)
        x-example: cards
      - name: activeOnly
        in: query
        required: true
        type: boolean
        description: Available values (true or false)
        x-example: true
      - name: customerId
        type: string
        required: false
        in: query
        description: customer_id
      - name: operation
        type: string
        in: query
        description: Operation value that need to be considered for filter
        required: false
        enum :
        - contracts
        - transferFrom
        - transferTo
        - makePayment
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/_GetGlobalPositionResponse'
        204:
          description: No Content
        400:
          description: Bad Request
          schema:
            $ref: '#/definitions/Errors'
        401:
          description: Unauthorized
          schema:
            $ref: '#/definitions/Errors'
        403:
          description: Forbidden
          schema:
            $ref: '#/definitions/Errors'
        404:
          description: Not Found
          schema:
            $ref: '#/definitions/Errors'
        500:
          description: Internal Server Error
          schema:
            $ref: '#/definitions/Errors'
        503:
          description: Service Unavailable
          schema:
            $ref: '#/definitions/Errors'
        504:
          description: Gateway Service Timeout
          schema:
            $ref: '#/definitions/Errors'
      security:
      - JWT Profile:
        - gblpos.read
        x-IBM-Client-Id: []
  /refresh:
    post:
      description: This operation performs a manual update of the global position information of one particular user bank. This operation allows us to obtain total balances by product families and contract balances. You can group balances by product family. It allows filters by family of products, by contractual situation (active, canceled).
        - if familyProduct is informed, the return will be only for this family -
        If active_only is true only active contracts take it, with the false return there will be active and canceled contracts.
      summary: Performs manual global position bank update and provides information about contracts and balances of contracts of a
        customer.
      tags:
      - global-position
      deprecated: false
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: bank_id
        type: string
        required: true
        in: query
        description : "Unique bank ID"
        x-example : "AAOZ"
      - name: familyProduct
        in: query
        required: false
        type: string
        description: Available values (accounts, cards, loans, deposits and securities)
        x-example: cards
      - name: activeOnly
        in: query
        required: true
        type: boolean
        description: Available values (true or false)
        x-example: true
      - name: customerId
        type: string
        required: false
        in: query
        description: customer_id
      - name: operation
        type: string
        in: query
        description: Operation value that need to be considered for filter
        required: false
        enum :
        - contracts
        - transferFrom
        - transferTo
        - makePayment
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/_GetGlobalPositionResponse'
        204:
          description: No Content
        400:
          description: Bad Request
          schema:
            $ref: '#/definitions/Errors'
        401:
          description: Unauthorized
          schema:
            $ref: '#/definitions/Errors'
        403:
          description: Forbidden
          schema:
            $ref: '#/definitions/Errors'
        404:
          description: Not Found
          schema:
            $ref: '#/definitions/Errors'
        500:
          description: Internal Server Error
          schema:
            $ref: '#/definitions/Errors'
        503:
          description: Service Unavailable
          schema:
            $ref: '#/definitions/Errors'
        504:
          description: Gateway Service Timeout
          schema:
            $ref: '#/definitions/Errors'
      security:
      - JWT Profile:
        - gblpos.read
        x-IBM-Client-Id: []

  /view/{customer_id}/:
    put:
      description: Through  this operation the client can customize their global position,
        define an alias for their contracts and cards, manage their visibility, as
        well as the order in which contracts and cards are displayed.
      summary: Customization (show/hide a contract, alias assignment) of the list
        of contracts a customer can see on his Global Position. Contract order sorting
        of product categories on the  Global Position for a customer.
      tags:
      - global-position
      operationId: MeContractNewAliasPutV3
      deprecated: false
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: body
        in: body
        required: true
        description: contractsView
        schema:
          $ref: '#/definitions/_GlobalPositionView'
      - name: customer_id
        type: string
        required: true
        in: path
        description: Customer unique identifier.
      - name: bank_id
        type: string
        required: true
        in: query
        description : "Unique bank ID"
        x-example : "AAOZ"
      responses:
        201:
          description: Created
          schema:
            $ref: '#/definitions/_GlobalPositionView'
        400:
          description: Bad Request
          schema:
            $ref: '#/definitions/Errors'
        401:
          description: Unauthorized
          schema:
            $ref: '#/definitions/Errors'
        403:
          description: Forbidden
          schema:
            $ref: '#/definitions/Errors'
        404:
          description: Not Found
          schema:
            $ref: '#/definitions/Errors'
        415:
          description: Unsupported Media Type
          schema:
            $ref: '#/definitions/Errors'
        500:
          description: Internal Server Error
          schema:
            $ref: '#/definitions/Errors'
        503:
          description: Service Unavailable
          schema:
            $ref: '#/definitions/Errors'
        504:
          description: Gateway Service Timeout
          schema:
            $ref: '#/definitions/Errors'
      security:
      - JWT Profile:
        - gblpos.view
        x-IBM-Client-Id: []
    get:
      description: It is desired that the client can see the list of contracts available
        for their global position, in order to show the actual customization.
      summary: Retrieve a full views of contracts available for all user on the Global
        Position along with their customization settings (show/hide a contract, alias,  family
        product order).
      tags:
      - global-position
      operationId: MeContractNewAliasGetV3
      deprecated: false
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: customer_id
        type: string
        required: true
        in: path
        description: Customer unique identifier.
      - name: bank_id
        type: string
        required: true
        in: query
        description : "Unique bank ID"
        x-example : "AAOZ"
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/_GlobalPositionView'
        400:
          description: Bad Request
          schema:
            $ref: '#/definitions/Errors'
        401:
          description: Unauthorized
          schema:
            $ref: '#/definitions/Errors'
        403:
          description: Forbidden
          schema:
            $ref: '#/definitions/Errors'
        404:
          description: Not Found
          schema:
            $ref: '#/definitions/Errors'
        500:
          description: Internal Server Error
          schema:
            $ref: '#/definitions/Errors'
        503:
          description: Service Unavailable
          schema:
            $ref: '#/definitions/Errors'
        504:
          description: Gateway Service Timeout
          schema:
            $ref: '#/definitions/Errors'
      security:
      - JWT Profile:
        - gblpos.view
  /monthly_balance/{from_month}:
    get:
      summary: Returns user monthly balances for all the user accounts. It has a query param to indicate from when update do you want to receive the data.
      tags:
      - balance
      produces:
      - '*/*'
      parameters:
      - $ref: '#/parameters/authorization'
      - $ref: '#/parameters/language'
      - $ref: '#/parameters/accept-language'
      - $ref: '#/parameters/device-information'
      - $ref: '#/parameters/santander-client-id'
      - $ref: '#/parameters/content-type'
      - $ref: '#/parameters/accept'
      - $ref: '#/parameters/x-ibm-client-id'
      - $ref: '#/parameters/x-b3-traceid'
      - $ref: '#/parameters/x-b3-parentspanid'
      - $ref: '#/parameters/x-b3-spanid'
      - $ref: '#/parameters/x-b3-sampled'
      - $ref: '#/parameters/x-santander-audit'
      - $ref: '#/parameters/trusted-device-app-id'
      - $ref: '#/parameters/santander-cipher-type'
      - $ref: '#/parameters/santander-cipher-seed'
      - $ref: '#/parameters/trusted-device-time'
      - $ref: '#/parameters/trusted-device-params'
      - name: from_month
        type: string
        required: true
        in: path
        description: Date with the format MMYYYY to set the initial date up today referencing response data.
        x-example: "052022"
      responses:
        200:
          description: OK
          schema:
            $ref: '#/definitions/_AccountsMonthlyBalanceResponseDTO'
        401:
          description: Unauthorized
        403:
          description: Forbidden
        404:
          description: Not Found
      deprecated: false
definitions:

#################
# Main Wrappers #
#################

  _AccountsMonthlyBalanceResponseDTO:
    description: "Data structure containing all the information related to the customer position view."
    type: object
    properties:
      accountsMonthlyBalance:
        description: "list containing the monthly balances"
        type: array
        items:
          $ref:  "#/definitions/_AccountMonthlyBalanceItem"

  _AccountMonthlyBalanceItem:
    description: "Data structure containing all the information related to the customer position view."
    type: object
    properties:
      account:
        $ref:  "#/definitions/_AccountMonthlyBalance"
      monthlyBalance:
        description: "monthly balance for the account specified in the 'accout' parameter"
        type: array
        items:
          $ref:  "#/definitions/_MonthlyBalanceItem"

  _MonthlyBalanceItem:
    description: "Monthly balance"
    type: object
    properties:
      month:
        $ref:  "#/definitions/Order"
      balance:
        $ref:  "#/definitions/Amount"
      expense:
        $ref:  "#/definitions/Amount"
      income:
        $ref:  "#/definitions/Amount"

  _AccountMonthlyBalance:
    x-bizObjRef: Account
    type : "object"
    description : "Data structure containing account information"
    properties :
       accountId :
          description : "Account number.\n\nThe value uses the format defined in 'accountIdType'."
          type : "string"
          example : "00491655300123456700"

  _GlobalPositionView:
    description: "Data structure containing all the information related to the customer position view."
    type: object
    properties:
      accountsView:
        $ref:  "#/definitions/_AccountsViewInformation"
      cardsView:
        $ref:  "#/definitions/_CardsViewInformation"
      loansView:
        $ref:  "#/definitions/_LoansViewInformation"
      depositsView:
        $ref:  "#/definitions/_DepositsViewInformation"
      pensionsView:
        $ref:  "#/definitions/_PensionsViewInformation"
      securitiesView:
        $ref:  "#/definitions/_SecuritiesViewInformation"
      investmentFundsView:
        $ref:  "#/definitions/_InvestmentFundsViewInformation"

  _GetGlobalPositionResponse:
    description: "Data structure containing all the information related to the customer position."
    type: object
    properties:
      accounts:
        $ref:  "#/definitions/_AccountsInformation"
      cards:
        $ref:  "#/definitions/_CardsInformation"
      loans:
        $ref:  "#/definitions/_LoansInformation"
      deposits:
        $ref:  "#/definitions/_DepositsInformation"
      pensions:
        $ref:  "#/definitions/_PensionsInformation"
      securities:
        $ref:  "#/definitions/_SecuritiesInformation"
      investmentFunds:
        $ref:  "#/definitions/_InvestmentFundsInformation"
      insurances:
        $ref:  "#/definitions/_InsuranceInformation"

#Wrappers for account view
  _AccountsViewInformation:
    description: Data structure containing account information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      accounts:
        description: Array of accounts
        type: array
        items:
          $ref:  "#/definitions/_AccoutViewDetails"

  _AccoutViewDetails:
    x-bizObjRef: Account
    type : "object"
    description : "Data structure containing account information"
    properties :
      accountId :
        description : "Account number.\n\nThe value uses the format defined in 'accountIdType'."
        type : "string"
        example : "abcdefghijklmnopqrstuvwxyz"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY CHEQUING ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for card view
  _CardsViewInformation:
    description: Data structure containing card information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      cards:
        description: Array of cards
        type: array
        items:
          $ref:  "#/definitions/_CardViewDetails"

  _CardViewDetails:
    x-bizObjRef: Card
    type : "object"
    description : "Data structure containing card details"
    properties :
      cardId :
        type : "string"
        description : "Card ID that is used to uniquely identify the card in card-related operations. For internal use, the value does not require tokenization.\n\nThe value is usually the same as the card number."
        example : "123456789-0"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "Card for family expenses"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for loan view
  _LoansViewInformation:
    description: Data structure containing loan information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      loans:
        description: Array of loans
        type: array
        items:
          $ref:  "#/definitions/_LoanViewDetails"

  _LoanViewDetails:
    x-bizObjRef: Loan
    description : "Data structure containing information about a loan"
    type : "object"
    properties :
      loanId :
        type : "string"
        description : "Unique loan ID"
        example : "123563-ffet-3556"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY LOAN ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for deposit view
  _DepositsViewInformation:
    description: Data structure containing deposit information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      deposits:
        description: Array of deposits
        type: array
        items:
          $ref:  "#/definitions/_DepositViewDetails"

  _DepositViewDetails:
    x-bizObjRef: Deposit
    type : "object"
    description : "Data structure containing information about a deposit"
    properties :
      depositId :
        type : "string"
        description : "Unique deposit ID"
        example : "123563-ffet3556"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY DEPOSIT ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for pension plan view
  _PensionsViewInformation:
    description: Data structure containing pension information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      pensionPlans:
        description: Array of pension plans
        type: array
        items:
          $ref:  "#/definitions/_PensionViewDetails"

  _PensionViewDetails:
    x-bizObjRef: Pension
    type : "object"
    description : "Data structure containing information about a pension plan"
    properties :
      pensionId :
        type : "string"
        description : "Unique pension plan ID"
        example : "123563-ffet3556"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY PENSION ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for security view
  _SecuritiesViewInformation:
    description: Data structure containing security information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      securities:
        description: Array of securities
        type: array
        items:
          $ref:  "#/definitions/_SecurityViewDetails"

  _SecurityViewDetails:
    x-bizObjRef: Security
    type : "object"
    description : "Data structure containing information about a security"
    properties :
      securityId :
        type : "string"
        description : "Security ID"
        example : "123563-ffet3556"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY SECURITY ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : true

#Wrappers for investment funds view
  _InvestmentFundsViewInformation:
    description: Data structure containing investment funds information
    type: object
    properties:
      presentationOrderPoducts:
        $ref:  "#/definitions/Order"
      investmentFunds:
        description: Array of loans
        type: array
        items:
          $ref:  "#/definitions/_InvestmentFundViewDetails"

  _InvestmentFundViewDetails:
    x-bizObjRef: InvestmentFund
    description : "Data structure containing information about an investment fund"
    type : "object"
    properties :
      investmentFundId :
        type : "string"
        description : "Investment fund ID"
        example : "1532928024698765"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY INVESTMENT FUND"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
          presentationOrder :
            type : "integer"
            description : "Display order of the contract within the product family-specific list.\n\nThe possible values are incremental whole digits from 0:\n- All contracts with 0 are listed first, using an internal contract ID to determine their mutual order\n- Contracts with 1, 2... are listed next in the order defined\n- Any contracts with no defined display order are listed at the end, using an internal contract ID to determine their mutual order\n\nThe default value is 0."
            example : 1
          visibilityIndicator :
            type : "boolean"
            description : "Contract visibility indicator.\n\nThe possible values are:\n- true = Contract is shown in the customer's global position\n- false = Contract is hidden in the customer's global position\n\nThe default value is true."
            example : tru

#Global Position Insurance funds Wrappers
  _InsuranceInformation:
    description: "Data structure containing aggregated information from the Insurance balances and broken down to Insurance level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_InsuranceTotalBalances"
      list:
        description: "Array of investments."
        type: array
        items:
          $ref: "#/definitions/_InsuranceWithLinks"

  _InsuranceWithLinks:
    type: object
    description: "Data structure containing Insurance information and interest links."
    properties:
      insurance:
        $ref:  "#/definitions/Insurance"
      insuranceDetailsLink:
        $ref: "#/definitions/_InsuranceDetailsLink"

  _InsuranceTotalBalances:
    description: "Data structure containing aggregated information from the investment balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"

#Global Position Account Wrappers
  _AccountsInformation:
    description: "Data structure containing aggregated information from the account balances and broken down to account level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_AccountTotalBalances"
      list:
        description: "Array of accounts."
        type: array
        items:
          $ref: "#/definitions/_AccountWithLinks"

  _AccountWithLinks:
    type: object
    description: "Data structure containing account information and interest links."
    properties:
      account:
        $ref:  "#/definitions/Account"
      accountDetailsLink:
        $ref: "#/definitions/_AccoutDetailsLink"
      transactionsListLink:
        $ref: "#/definitions/_AccountTransactionsListLink"

  _AccountTotalBalances:
    description: "Data structure containing aggregated information from the account balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"
      totalWithholding:
        $ref:  "#/definitions/Amount"
      totalAvailable:
        $ref:  "#/definitions/Amount"

#Global Position Card Wrappers
  _CardsInformation:
    description: "Data structure containing information broken down to card level."
    type: object
    properties:
      list:
        description: "Array of cards."
        type: array
        items:
          $ref: "#/definitions/_CardWithLinks"

  _CardWithLinks:
    type: object
    description: "Data structure containing card information and interest links."
    properties:
      card:
        $ref:  "#/definitions/Card"
      cardDetailsLink:
        $ref: "#/definitions/_CardDetailsLink"
      statementsListLink:
        $ref: "#/definitions/_CardStatementsListLink"
      transactionsListLink:
        $ref: "#/definitions/_CardTransactionsListLink"

#Global Position Loans Wrappers
  _LoansInformation:
    description: "Data structure containing aggregated information from the loan balances and broken down to account level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_LoanTotalBalances"
      list:
        description: "Array of loans."
        type: array
        items:
          $ref: "#/definitions/_LoanWithLinks"

  _LoanWithLinks:
    type: object
    description: "Data structure containing loan information and interest links."
    properties:
      loan:
        $ref:  "#/definitions/Loan"
      loanDetailsLink:
        $ref: "#/definitions/_LoanDetailsLink"

  _LoanTotalBalances:
    description: "Data structure containing aggregated information from the loan balances."
    type: object
    properties:
      totalPrincipal:
        $ref:  "#/definitions/Amount"
      totalAmortization:
        $ref:  "#/definitions/Amount"

#Global Position Deposits Wrappers
  _DepositsInformation:
    description: "Data structure containing aggregated information from the deposit balances and broken down to deposit level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_DepositTotalBalances"
      list:
        description: "Array of deposits."
        type: array
        items:
          $ref: "#/definitions/_DepositWithLinks"

  _DepositWithLinks:
    type: object
    description: "Data structure containing deposit information and interest links."
    properties:
      deposit:
        $ref:  "#/definitions/Deposit"
      depositDetailsLink:
        $ref: "#/definitions/_DepositDetailsLink"

  _DepositTotalBalances:
    description: "Data structure containing aggregated information from the deposit balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"
      totalProfit:
        $ref:  "#/definitions/Amount"

#Global Position Pensions Wrappers
  _PensionsInformation:
    description: "Data structure containing aggregated information from the pension plan balances and broken down to pension plan level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_PensionTotalBalances"
      list:
        description: "Array of pension plans."
        type: array
        items:
          $ref: "#/definitions/_PensionWithLinks"

  _PensionWithLinks:
    type: object
    description: "Data structure containing pension plan information and interest links."
    properties:
      pension:
        $ref:  "#/definitions/Pension"
      pensionDetailsLink:
        $ref: "#/definitions/_PensionDetailsLink"

  _PensionTotalBalances:
    description: "Data structure containing aggregated information from the pension plan balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"

#Global Position Securities Wrappers
  _SecuritiesInformation:
    description: "Data structure containing aggregated information from the security balances and broken down to security level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_SecurityTotalBalances"
      list:
        description: "Array of securities."
        type: array
        items:
          $ref: "#/definitions/_SecurityWithLinks"

  _SecurityWithLinks:
    type: object
    description: "Data structure containing security information and interest links."
    properties:
      security:
        $ref:  "#/definitions/Security"
      securityDetailsLink:
        $ref: "#/definitions/_SecurityDetailsLink"

  _SecurityTotalBalances:
    description: "Data structure containing aggregated information from the security balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"

#Global Position Investment funds Wrappers
  _InvestmentFundsInformation:
    description: "Data structure containing aggregated information from the investment balances and broken down to investment level."
    type: object
    properties:
      totalBalances:
        $ref:  "#/definitions/_InvestmentFundTotalBalances"
      list:
        description: "Array of investments."
        type: array
        items:
          $ref: "#/definitions/_InvestmentFundWithLinks"

  _InvestmentFundWithLinks:
    type: object
    description: "Data structure containing investment information and interest links."
    properties:
      investmentFund:
        $ref:  "#/definitions/InvestmentFund"
      investmentFundDetailsLink:
        $ref: "#/definitions/_InvestmentFundDetailsLink"

  _InvestmentFundTotalBalances:
    description: "Data structure containing aggregated information from the investment balances."
    type: object
    properties:
      totalCurrent:
        $ref:  "#/definitions/Amount"

  _InsuranceDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/investment_funds/001578658029699345'

##########################
# LinkReference Wrappers #
##########################
  _AccoutDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/accounts/09012781018079'

  _AccountTransactionsListLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/accounts/09012781018079/transactions'

  _CardDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/cards/4547420008209985'

  _CardStatementsListLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/cards/4547420008209985/statements'

  _CardTransactionsListLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/cards/4547420008209985/transactions'

  _LoanDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/loans/001520891037864384'

  _DepositDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/deposits/001520893020013689'

  _PensionDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/pensions/001578653139876456'

  _SecurityDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/securities/001532924007658821'

  _InvestmentFundDetailsLink:
    x-bizObjRef: LinkReference
    type : "object"
    description : "Data structure containing link information"
    properties :
       href :
          type : "string"
          format : "uri"
          description : "Reference link url, that allows to navigate to specified uri"
          example: 'https://www.host.com/investment_funds/001578658029699345'

######################
# Dictionary Objects #
######################
  Account :
    type : "object"
    description : "Data structure containing account information"
    properties :
      accountId :
        description : "Account number.\n\nThe value uses the format defined in 'accountIdType'."
        type : "string"
        example : "9012781018079"
      accountIdentification :
        description : "Data structure containing information related to account identification"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed account number"
              type : "string"
              example : "00***384"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
            type : "string"
            description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
            example : "MY CHECKING ACCOUNT"
          isMainItem :
            type : "boolean"
            description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
            example : true
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              description :
                type : "string"
                description : "Description of the goods and/or services of a trade transaction"
                example : "TRADITIONAL ACCOUNT"
                maxLength : 840
              typeDescription :
                type : "string"
                description : "Product type description.\n\nThe possible values are: \n- Current Account\n- Saving Account\n- Credit Account"
                maxLength : 50
                example : "TRADITIONAL_ACCOUNT"
      statusInfo :
          $ref : "#/definitions/StatusInfo"
      balances:
        $ref:  "#/definitions/AccountBalances"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  AccountBalances :
    type : object
    description : "Data structure containing information related to different balances."
    properties :
      current :
        description : "Actual account balance at the query moment."
        type : "object"
        properties :
            amount :
              $ref : "#/definitions/Amount"
            lastUpdateDate :
              type : "string"
              format : "date"
              description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
              example : "2020-12-01"
      withholding :
        description : "Sum of all active withholdings on the account. This balance decreases the available balance. "
        type : "object"
        properties :
          amount :
            $ref : "#/definitions/Amount"
          lastUpdateDate :
              type : "string"
              format : "date"
              description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
              example : "2020-12-01"
      available :
          description : "Current balance plus overdraft balance less withholding balance less pending to authorize and less pending to consolidate."
          type : "object"
          properties :
            amount :
              $ref : "#/definitions/Amount"
            lastUpdateDate :
              type : "string"
              format : "date"
              description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
              example : "2020-12-01"
      overdraft :
          description : "Accumulated active overdraft balances. This balance increases the available balance."
          type : "object"
          properties :
            amount :
                $ref : "#/definitions/Amount"
            lastUpdateDate :
                type : "string"
                format : "date"
                description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
                example : "2020-12-01"
      overdraftUsed :
          description : "Used overdraft balances."
          type : "object"
          properties :
            amount :
                $ref : "#/definitions/Amount"
            lastUpdateDate :
                type : "string"
                format : "date"
                description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
                example : "2020-12-01"

  Card :
    type : "object"
    description : "Data structure containing card details"
    properties :
      cardId :
        type : "string"
        description : "Card ID that is used to uniquely identify the card in card-related operations. For internal use, the value does not require tokenization.\n\nThe value is usually the same as the card number."
        example : "4547420008209985"
      cardIdentification :
        description : "Data structure containing identification information for a card"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed card number"
              type : "string"
              example : "xxxx-xxxx-xxxx-5643"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "Card for family expenses"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      type :
        type : "string"
        description : "Card type that defines the operations for which it can be used"
        example : "CREDIT CARD"
      typeCode :
        description : "Card type code"
        type : "string"
        example : "01"
      typeDescription :
        description : "Card type description"
        type : "string"
        example : "Visa Oro 123"
      isOperable :
          type : "boolean"
          description : "Whether the card is operable and that the customer can use it"
          example : true
      embossing :
        description : "Data structure containing information embossed on the card"
        type : "object"
        properties :
          code :
            type : "string"
            description : "Code that is printed on the card"
            example : "495021010000000974848"
          description :
            type : "string"
            description : "Description of the embossing code used on the card"
            example : "PREMIUM CREDIT PR"
      embossing2 :
        description : "Data structure containing information embossed on the card. Poland requies a second field (emboss2) just for long names when emboss1 is not enought"
        type : "object"
        properties :
          code :
            type : "string"
            description : "Code that is printed on the card"
            example : "495021010000000974848"
          description :
            type : "string"
            description : "Description of the embossing code used on the card"
            example : "PREMIUM CREDIT PR"
      isRoamingActive:
        type : "boolean"
        description : "Whether roaming is enabled"
        example : true
      isWalletAllowed:
        type : "boolean"
        description : "Whether the card can be used with wallet apps"
        example : true
      statusInfo :
        description : "Data structure containing information about the status of an item"
        type : "object"
        properties :
          statusDescription :
            type : "string"
            description : "Status description"
            example : "INACTIVE"
          statusReasonDescription :
            type : "string"
            description : "Description of the status reason"
            example : "Stolen"
      expiration :
        $ref : "#/definitions/Expiration"
      balances:
        $ref : "#/definitions/CardBalances"
      creditLimit :
        $ref : "#/definitions/Amount"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  CardBalances :
    type : "object"
    description : "Data structure containing information about different card balances"
    properties :
       available :
          $ref : "#/definitions/Balance"
       disposed :
          $ref : "#/definitions/Balance"

  Loan :
    description: Data structure containing information about a loan
    type: object
    properties:
      loanId:
        type: string
        description: Unique loan ID
        example: '1520891037864384'
      loanIdentification:
        description : "Data structure containing identification information for a loan"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed loan number"
              type : "string"
              example : "00***384"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY LOAN ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              typeDescription :
                type : "string"
                description : "Product type description"
                maxLength : 50
                example : "Credit Line"
      statusInfo:
        $ref: '#/definitions/StatusInfo'
      balances:
        $ref: '#/definitions/LoanBalances'
      nextPaymentAmount:
        $ref: '#/definitions/Amount'
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  LoanBalances :
    type : "object"
    description : "Data structure containing information about different loan balances"
    properties :
       principal :
          $ref : "#/definitions/Balance"
       amortized :
          $ref : "#/definitions/Balance"

  Deposit :
    description : "Data structure containing information about a deposit"
    type : "object"
    properties :
      depositId :
        type : "string"
        description : "Unique deposit ID"
        example : "1520893020013689"
      depositIdentification :
        description : "Data structure containing identification information for a deposit"
        type : "object"
        properties :
         displayNumber :
           description : "Displayed deposit number"
           type : "string"
           example : "00***384"
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              typeDescription :
                type : "string"
                description : "Product type description"
                maxLength : 50
                example : "FIXED-TERM"
          validityPeriod:
            type : "object"
            description : "Data structure containing period information"
            properties :
              endDate :
                type : "string"
                description : "Date when the period ends.\n\nThe value uses the complete data format defined in ISO 8601:\n\n'YYYY-MM-DD'\n\nWhere:\n- YYYY: 4-digit year\n- MM: 2-digit month (for example, 01 = January)\n- DD: 2-digit day of the month (01 through 31)"
                format : "date"
                example : "2025-12-01"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY DEPOSIT ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      statusInfo :
        $ref : "#/definitions/StatusInfo"
      balances :
        $ref : "#/definitions/DepositBalances"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  DepositBalances :
    type : "object"
    description : "Data structure containing information about different deposit balances"
    properties :
       current :
          $ref : "#/definitions/Balance"
       profit :
          $ref : "#/definitions/Balance"

  Pension :
    description : "Data structure containing information about a pension plan"
    type : "object"
    properties :
      pensionId :
        type : "string"
        description : "Unique pension plan ID"
        example : "1578653139876456"
      pensionIdentification :
        description : "Data structure containing identification information for a pension plan"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed pension plan number"
              type : "string"
              example : "00***384"
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              typeDescription :
                type : "string"
                description : "Product type description"
                maxLength : 50
                example : "Scheduled withdrawal"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY PENSION ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      statusInfo :
        $ref : "#/definitions/StatusInfo"
      balances :
        $ref : "#/definitions/PensionBalances"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  PensionBalances :
    type : "object"
    description : "Data structure containing information about different pension plan balances"
    properties :
       current :
          $ref : "#/definitions/Balance"

  Insurance:
      type: object
      description: "Data structure containing information about an insurance"
      properties:
        insuranceId:
            type : "string"
            description : "Unique insurance ID"
            example : "001520893020013689"
        channelAccessAgreementInformation :
          description : "Data structure containing information for a global position."
          type : "object"
          properties :
            alias :
                type : "string"
                description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
                example : "Family Insurance"
        insuranceIdentification :
            description : "Data structure containing identification information for an insurance policy"
            type : "object"
            properties :
              policyNumber :
                  description : "Insurance policy number"
                  type : "string"
                  example : "1234567890987654321"
        typeCode :
            description : "Insurance type code"
            type : "string"
            example : "A300"
        typeDescription :
            description : "Insurance type description"
            type : "string"
            example : "Family Insurance 123"
        currentAnnualPremium :
            description : "Data structure containing amount details"
            type : "object"
            properties :
              amount :
                  description : "Amount.\n\nThe value uses the data format defined in ISO 20022 and has a maximum of 18 digits, of which 5 can be decimals, separated by a point."
                  type : "number"
                  minimum : -1.0E13
                  maximum : 1.0E13
                  example : 15000.00
              currency :
                  description : "Currency code.\n\nThe value is in the alpha-3 format defined in ISO 4217 (https://www.iso.org/iso-4217-currency-codes.html)."
                  type : "string"
                  minLength : 3
                  maxLength : 3
                  pattern : (^.{3}$)
                  example : "GBP"
        link:
          type: object
          properties:
            href:
              type: string
              format: uri
              example: "http://www.host.com/v1/cards/00491655300123456700/transactions/1234567"
              description: "Reference link url, that allows to navigate to specified uri"
        bankId :
          type: string
          description : "Unique bank ID"
          x-example : "AAOZ"


  Security :
    description : "Data structure containing information about a security"
    type : "object"
    properties :
      securityId :
        type : "string"
        description : "Security ID"
        example : "1532924007658821"
      securityIdentification :
        description : "Data structure containing identification information for a security"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed security number"
              type : "string"
              example : "00***384"
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              description :
                type : "string"
                description : "Description of the goods and/or services of a trade transaction"
                example : "Santander Securities 123"
                maxLength : 840
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY SECURITY ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      statusInfo :
        $ref : "#/definitions/StatusInfo"
      balances :
        $ref : "#/definitions/SecurityBalances"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  SecurityBalances :
    type : "object"
    description : "Data structure containing information about different security balances"
    properties :
       current :
          $ref : "#/definitions/Balance"

  InvestmentFund :
    description : "Data structure containing information about an investment fund"
    type : "object"
    properties :
      investmentFundId :
        type : "string"
        description : "Investment fund ID"
        example : "1578658029699345"
      investmentFundIdentification :
        description : "Data structure containing identification information for an investment fund"
        type : "object"
        properties :
          displayNumber :
              description : "Displayed investment fund number"
              type : "string"
              example : "001578658029699345"
      contract :
        description : "Data structure containing contract information"
        type: object
        properties :
          product :
            type : "object"
            description : "Data structure containing product information. Products can be services or goods."
            properties :
              typeDescription :
                type : "string"
                description : "Product type description"
                maxLength : 50
                example : "INTERNATIONAL FUND"
      channelAccessAgreementInformation:
        description : "Data structure containing information for a global position."
        type : "object"
        properties :
          alias :
              type : "string"
              description : "Name (alias) that the customer has assigned to the item for easy identification. Only required for customer usability."
              example : "MY INVESTMENT FUND ACCOUNT"
          isMainItem :
              type : "boolean"
              description : "Whether the item is the customer's main item. Applies only when the operation is used by a physical or business customer, not when used by Santander staff.\n\nThe possible values are:\n- true = Main item\n- false = Secondary item"
              example : true
      statusInfo :
        $ref : "#/definitions/StatusInfo"
      balances :
        $ref : "#/definitions/InvestmentFundBalances"
      bankId :
        type: string
        description : "Unique bank ID"
        x-example : "AAOZ"

  InvestmentFundBalances :
    type : "object"
    description : "Data structure containing information about different investment fund balances"
    properties :
       current :
          $ref : "#/definitions/Balance"

  Balance :
    type : "object"
    description : "Numerical representation of the net increases and decreases in an account at a specific point in time."
    properties :
       amount :
          $ref : "#/definitions/Amount"
       lastUpdateDate :
          type : "string"
          format : "date"
          description : "Balance last update date. The value uses the complete data format defined in ISO 8601: 'YYYY-MM-DD' Where: - YYYY: 4-digit year - MM: 2-digit month (for example, 01 = January) - DD: 2-digit day of the month (01 through 31)"
          example : "2020-12-01"

  Expiration :
    description : "Data structure containing the expiration date"
    type : "object"
    properties :
       month :
          type : "string"
          description : "Month of expiry.\n\nThe value is a 2-digit month of the year."
          example : "05"
       year :
          type : "string"
          description : "Year of expiry.\n\nThe value is a 4-digit year."
          example : "2022"

  StatusInfo :
    description : "Data structure containing information about the status of an item"
    type : "object"
    properties :
       statusDescription :
          type : "string"
          description : "Status description"
          example : "ACTIVE"

  Amount :
    description : "Data structure containing amount details"
    type : "object"
    required :
    - amount
    - currency
    properties :
      amount :
        description : "Amount.\n\nThe value uses the data format defined in ISO 20022 and has a maximum of 18 digits, of which 5 can be decimals, separated by a point.\n\nRange value is between -1.0E13 and 1.0E13"
        type : "string"
        example : 99.95
      currency :
        description : "Currency code.\n\nThe value is in the alpha-3 format defined in ISO 4217 (https://www.iso.org/iso-4217-currency-codes.html)."
        type : "string"
        minLength : 3
        maxLength : 3
        pattern : (^.{3}$)
        example : "MXN"

  Order :
    type : "integer"
    description : "Order"
    example : 1

  Errors :
    type : "object"
    description : "Data structure containing the details for errors"
    properties :
       errors :
          description : "Array of Errors"
          type : "array"
          items :
             $ref : "#/definitions/Error"

  Error :
    type : "object"
    description : "Data structure containing the error details"
    properties :
       code :
          type : "string"
          description : "Unique alphanumeric human readable error code"
          example : "ERR001"
       message :
          type : "string"
          description : "Brief summary of the reported issue"
          example : "Invalid Action"
       level :
          type : "string"
          description : "Level of the reported issue"
          enum :
          - info
          - warning
          - error
          example : "error"
       description :
          type : "string"
          description : "Detailed description of the reported issue"
          example : "Description"

tags:
- name: global-position
  description: Bank Global Position Controller
x-ibm-configuration:
  enforced: true
  testable: true
  phase: realized
securityDefinitions:
  JWT Profile:
    type: oauth2
    description: OAuth Identify, JWT Profile
    flow: implicit
    scopes:
      gblpos.read: Global position read scope
      gblpos.view: Global position view scope
    authorizationUrl: https://api-sandbox.santander.com/santander/external/oauth/token
    x-tokenIntrospect:
      url: $(security-url)
  Security OAuth:
    type: oauth2
    description: Security OAuth
    flow: application
    x-tokenIntrospect:
      url: $(security-url)
    tokenUrl: "http://host.com"
  x-IBM-Client-Id:
    type: apiKey
    description: Client Id registered in API Portal
    in: header
    name: X-IBM-Client-Id
security:
- Security OAuth: []
  x-IBM-Client-Id: []
parameters:
  authorization:
    name: authorization
    type: string
    required: true
    in: header
    description: Field to send the access token to the API, initially OAuth and JWT.
  language:
    name: language
    type: string
    required: true
    in: header
    description: The server responds with the field in the header with the response language. Following the ([ISO 639-2]-[ISO 3166-1/Alpha3]) code standard.
    x-example: Spain will be es-ESP
  accept-language:
    name: accept-language
    type: string
    required: false
    in: header
    description: The consumer list of languages by order of preference. Following the (([ISO 639-2]-[ISO 3166-1/Alpha3])) code standard.
    x-example: Spain will be es-ESP
  device-information:
    name: device-information
    type: string
    required: true
    in: header
    description: >-
      CHANNEL/SEGMENT FRONT_APP/VERSION DEVICE OS/OS_VERSION NETWORK_CLIENT/VERSION FRAMEWORK/VERSION

      Channel: [ISO 639-2][] plus 5 custom contry digits. For
      Segment: retail / business
      Front_App/Version: For example OneApp/1.0
      Device: Platform value extracted from the device. For example iPhone13
      OS/VERSION: android / iOS / windows / linux / macOS, version is a platform value extracted from the device. For example iOS/1.0
      Network_Client/Version: App network client and version. For example CF_Network/5.1
      Framework/Version: For example Darwin/16.3.0
  santander-client-id:
    name: santander-client-id
    type: string
    required: true
    in: header
    description: The consumer application identifier for APIs. santander-client-id must be defined just as a Request header, this is not a header defined as an API Key at security definition level
  content-type:
    name: content-type
    type: string
    required: false
    in: header
    description: The server responds with the data format of the response. Backend expected a JSON document format.
    x-example: application/json
  #
  # STANDARD HEADER
  #
  accept:
    name: accept
    type: string
    required: false
    in: header
    description: The consumer request the data format of the response by order of preference.Backend expected a JSON document format.
    x-example: application/json
  x-ibm-client-id:
    name: x-ibm-client-id
    type: string
    required: false
    in: header
    description: The consumer application identifier (only for IBM API Connect). Legacy. Will be reviewed in future releases.
  #
  # TRACK HEADERS
  # Useful information at https://github.com/openzipkin/b3-propagation
  #
  x-b3-traceid:
    name: x-b3-traceid
    type: string
    required: false
    in: header
    description: The TraceId is 64 or 128-bit in length and indicates the overall ID of the trace. Every span in a trace shares this ID
    x-example: 463ac35c9f6413ad48485a3953bb6124
  x-b3-parentspanid:
    name: x-b3-parentspanid
    type: string
    required: false
    in: header
    description: The ParentSpanId is 64-bit in length and indicates the position of the parent operation in the trace tree. When the span is the root of the trace tree, there is no ParentSpanId.
    x-example: 0020000000000001
  x-b3-spanid:
    name: x-b3-spanid
    type: string
    required: false
    in: header
    description: The SpanId is 64-bit in length and indicates the position of the current operation in the trace tree. The value should not be interpreted. It may or may not be derived from the value of the TraceId
    x-example: a2fb4a1d1a96d312
  x-b3-sampled:
    name: x-b3-sampled
    type: string
    required: false
    in: header
    description: >-
      Sampling is a mechanism to reduce the volume of data that ends up in the tracing system. In B3, sampling applies consistently per-trace: once the sampling decision is made, the same value should be consistently sent downstream. This means you will see all spans sharing a trace ID or none.

      Accepted values are\n

      * 1. Means accepted\n
      * 0. Means denied

      Before this specification was written, some tracers propagated X-B3-Sampled as true or false as opposed to 1 or 0. While you shouldn't encode x-b3-sampled as true or false, a lenient implementation may accept them
    x-example: 1
  #
  # Only for UK
  #
  x-santander-audit:
    name: x-santander-audit
    type: string
    required: false
    in: header
    description: Extra information coming from the client. Needed for audit purposes
  #
  # Only for Poland
  #
  trusted-device-app-id:
    name: trusted-device-app-id
    type: string
    required: false
    in: header
    description: Device information permitted by operation for data query. Makes it possible to apply an experience base to optimize the data collection. This header is mandatory for Poland due to security reasons
  trusted-device-time:
    name: trusted-device-time
    type: string
    required: false
    in: header
    description: Device time formatted presenting the following the Standard Time Format pattern yyyy-MM-dd'T'HH:mm:ss.SSS'Z'
    x-example: "2022-04-12T15:55:39.407Z"
  trusted-device-params:
    name: trusted-device-params
    type: string
    required: false
    in: header
    description: Contains encripted parameters from the trusted device parameter source.
  #
  # SECURITY HEADERS
  #
  santander-cipher-type:
    name: santander-cipher-type
    type: string
    required: false
    in: header
    description: >-
      Indicates what kind of cipher algorithmic approach will be applied to a response fields subset.

      Supported values:
      * asymmetric
  santander-cipher-seed:
    name: santander-cipher-seed
    type: string
    required: false
    in: header
    description: Seed value applied on both sides to encrypt/decrypt response entity fields. This field is mandatory if Santander-Cipher-Type is presented and is asymetric in the request Headers collection.
x-ibm-endpoints:
- endpointUrl: https://apigw.extexpintcons.totta.pre.corp/sanpt/external-intcons
  description: APIMgmt_A99DDCFA34
  type:
  - production
  - development
...
